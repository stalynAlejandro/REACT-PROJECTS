export enum TABS {
    FROM = "FROM",
    TO = "TO",
}

export enum DATE_TYPE {
    START_DATE = "START_DATE",
    END_DATE = "END_DATE",
}

export const initialDatesValues = {
    startDate: null,
    endDate: null,
};

import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";

import Loading from "../../components/loading";

<Meta title="Loading" component={Loading} />

export const Template = (args) => {
    return <Loading {...args} />;
};

# Loading

El `Loading` es un componente de carga estándar.

<Story
    name="Loading"
    argTypes={{
        locale: {
            control: {
                type: "select",
                options: ["es-ES", "ca-ES", "en-US", "en-GB"],
            },
        },
        message: {
            control: {
                type: "text",
            },
        },
    }}
    args={{
        locale: "en-GB",
        message: "A custom message",
    }}
>
    {Template.bind({})}
</Story>

Los diferentes `props` que puede recibir son los siguietnes.

| **Prop**  | **Tipo** | **Valor por defecto** | **Descripción**                                          |
| :-------- | :------- | :-------------------- | :------------------------------------------------------- |
| `locale`  | `string` | `en-GB`               | Determina el idioma                                      |
| `message` | `string` | `undefined`           | Determina el texto que saldrá debajo del estado de carga |
